# Python build artifacts
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# Benchmark run outputs (regenerated by `make bench`)
runs/

# Local editor / tooling config
.claude/
.vscode/
.idea/

# Personal research notes (not part of the released artifact)
Local_LLM_memo.txt

# AWS sandbox infrastructure: state and account-specific outputs.
# These identify a real (torn-down) AWS account and are NOT needed to
# reproduce the offline benchmark. Excluded from the public artifact.
tf_outputs.json
aws/.terraform/
aws/terraform.tfstate
aws/terraform.tfstate.*
aws/*.tfstate
aws/*.tfstate.*
aws/*.zip
